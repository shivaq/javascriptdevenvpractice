// script で、babel-node を使うことで、babel によって transpile される
// babel を使うおかげで、commonJS の require ではなく、import でインポートできる
import chalk from "chalk";

// eslint-disable-next-line no-console
console.log(chalk.blue("Dev モードでアプリをスタートします。。。"));
